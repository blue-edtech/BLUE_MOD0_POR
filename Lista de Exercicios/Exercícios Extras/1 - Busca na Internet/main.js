// leitura da quantidade de cliques no terceiro link
const t = parseInt(prompt("Informe o número de cliques no terceiro link:"));

// cálculo dos cliques no primeiro link
const c1 = t * 4;

// exibição do resultado
console.log(`O número de cliques no primeiro link é ${c1}.`);
